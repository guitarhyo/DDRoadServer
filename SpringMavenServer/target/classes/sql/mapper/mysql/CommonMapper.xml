<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CommonDao">

<select id="selectBoard" resultType="map">
	select * from BOARD
</select>

<select id="selectUserList" resultType="com.ddroad.model.UserVO">
	select * from USERS
</select>

<select id="selectById" resultType="int" parameterType="com.ddroad.model.UserVO">
	select count(*) from USERS where id=#{id}
</select>

<insert id="join" parameterType="com.ddroad.model.UserVO">
	INSERT INTO USERS VALUES (#{id}, #{email}, #{nickname},#{gender}, #{age}, #{name})
</insert>

<insert id="write" parameterType="com.ddroad.model.BoardVO">
	INSERT INTO BOARD (title,writer,nickname,contents) VALUES (#{title}, #{writer}, #{nickname}, #{content})
	
</insert>
</mapper>